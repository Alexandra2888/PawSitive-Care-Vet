import { describe, it } from 'vitest';
import { render} from '@testing-library/react';
import {Faq} from '../../faq';


describe('test Input component', () => {
it('test if Input component is rendered', () => {
render(
    <Faq />
  );
})
})