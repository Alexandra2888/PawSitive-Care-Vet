import { describe, it } from 'vitest';
import { render} from '@testing-library/react';
import {Map} from "../../map";

describe('test Map component', () => {
it('test if Map component is rendered', () => {
render(
    <Map />
 );
})
})